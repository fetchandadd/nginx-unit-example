# Nginx Unit Example

Install and start nginx unit via docker
```sh
docker-compose up
```

Configure nginx unit
```sh
cd config
./configure.bash
```

PHP
```sh
http localhost:8081
```

Python
```sh
http localhost:8082
```
